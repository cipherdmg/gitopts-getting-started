{{- if empty .Values.decisionServerConsole.webConfigRef }}
apiVersion: v1
kind: ConfigMap
metadata:
  name:  {{ template "odm.ds-console-web-configmap.fullname" . }}
  labels:
{{ include "odm-additional-labels" (dict "componentName" "decisionServerConsole" "root" .) | indent 4 }}
data:
  web-configuration.properties: |
    # Add Web.xml context-param properties below
{{- end }}
